{
  "name": "parser-core",
  "version": "1.0.0",
  "main": "build/index.js",
  "scripts": {
    "lint": "echo lint",
    "build": "shadow-cljs release parser-core",
    "watch": "echo watch",
    "test": "shadow-cljs compile test",
    "clean": "rm -rf node_modules/ && yarn clean-build",
    "clean-build": "rm -rf build/ test-build/ .shadow-cljs/"
  },
  "devDependencies": {
    "shadow-cljs": "^2.8.93",
    "source-map-support": "^0.5.16"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.3"
  }
}
