{
  "name": "@symbio/barcode-processor-core",
  "version": "1.0.0",
  "main": "build/index.js",
  "scripts": {
    "lint": "tsc && eslint . --ext js,jsx,ts,tsx",
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "jest",
    "clean": "rm -rf ./build",
    "clean-all": "yarn clean && rm -rf node_modules/"
  },
  "dependencies": {
    "fp-ts": "^2.5.3",
    "io-ts": "^2.1.2",
    "ramda": "^0.27.0"
  },
  "devDependencies": {
    "@symbio/ts-scripts": "1.0.0",
    "@types/ramda": "^0.27.0"
  }
}
