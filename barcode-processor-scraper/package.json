{
  "name": "@symbio/barcode-processor-scraper",
  "version": "1.0.0",
  "main": "build/index.js",
  "scripts": {
    "lint": "tsc && eslint . --ext js,jsx,ts,tsx",
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "yarn lint && jest",
    "clean": "rm -rf ./build",
    "clean-all": "yarn clean && rm -rf node_modules/"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.3",
    "@symbio/ts-lib": "1.0.0",

    "@symbio/barcode-processor-core": "1.0.0"
  },
  "devDependencies": {
    "@types/cheerio": "^0.22.17",
    "@symbio/ts-scripts": "1.0.0"
  }
}
