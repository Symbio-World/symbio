{
  "name": "fetcher-core",
  "version": "1.0.0",
  "main": "build/index.js",
  "scripts": {
    "lint": "tsc --noEmit && eslint . --quiet --fix",
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "jest",
    "clean": "rm -rf ./build",
    "clean-all": "yarn clean && rm -rf node_modules/"
  },
  "devDependencies": {
    "@types/ramda": "0.26.43",
    "intermock": "0.2.2",
    "ts-scripts": "1.0.0"
  },
  "dependencies": {
    "ramda": "0.27.0"
  }
}
